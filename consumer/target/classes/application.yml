spring:
  application:
    name: provider
server:
  port: 9091

dubbo:
  application:
    name: consumer
  registry:
    address: zookeeper://127.0.0.1:2181

  #protocol:
   # port: 2080
    #name: dubbo
  #provider:
   # timeout: 1000